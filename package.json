{
  "name": "medalert-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "cd PRD && npm ci --no-audit --no-fund && npm run build",
    "dev": "cd PRD && npm run dev",
    "preview": "cd PRD && npm run preview"
  }
}
